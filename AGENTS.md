# Aimili Gateway 开发与部署边界

本文件约束本仓库的后续开发、验证与生产操作。先恢复用户当前操作，再做长期加固；不得把小改动自动扩大为跨服务发布。

## 变更分级

| 级别 | 典型范围 | 默认验证 | 默认生产范围 |
|---|---|---|---|
| UI | Vue 展示、文案、交互 | 定向前端测试与 `npm run build --prefix web` | 不连接 VPS |
| Gateway | HTTP API、数据库、编排逻辑，且不改运行时协议 | 定向 Go 测试；提交前运行受影响模块测试 | 单次 Gateway 健康检查；不重建 3x-ui |
| 运行时 | AimiliVPN、3x-ui/Xray、协议、端口、路由或来源策略 | 正式设计/计划中的定向测试与真实链路验证 | 经批准后阶梯部署与回滚检查 |

若实际影响高于开始时的分级，先说明新增影响、原因和最小验证范围；未获确认不得扩大。

## 执行纪律

- 在 Windows 开发任务中，禁止使用裸命令 `sh` 执行 Shell 脚本；`sh` 可能被 Snap Hutao Remastered 注册的应用执行别名接管。需要检查或执行脚本时，明确使用本机 Git Shell：`D:\SoftWare\Git\bin\sh.exe`，例如：`D:\SoftWare\Git\bin\sh.exe -n deploy/vps/install.sh`。
- UI 变更不得默认 SSH、构建 3x-ui、运行 Docker 或进行五分钟生产观察。
- Gateway 非协议变更不得默认修改 AimiliVPN、3x-ui/Xray 或其非 Gateway 受管资源。
- 同一 SSH、上传、安装或探测失败两次后，停止重复操作，读取最新错误并固定第一处失败边界。
- 部署回滚后不得直接重放安装；先只读确认当前二进制、服务、健康与回滚原因，再验证前置条件。
- 每台 Gateway VPS 的每次部署只创建并保留一份部署前备份；新备份前先核对现有备份，避免按时间累积重复副本。清理历史备份时仅删除已确认不再需要的备份目录，不删除当前部署、数据库或运行时数据。
- 远程检查应合并为一个脱敏的只读命令；只有状态变化或需要验证新假设时才重复检查。
- Git 只在开始、提交前和推送后检查；不得把反复 `status`/`fetch` 当作验证。
- 先执行单一 RED 测试、最小实现、受影响模块回归；全量测试仅在提交、跨模块变更或部署前运行。
- 每 45–90 分钟报告：当前范围、已验证证据、阻塞、下一步；发生范围升级或生产写入前立即报告。

## Aimili Gateway 前端浏览器验收

- 对部署在任意 VPS 上的 Aimili Gateway 前端进行登录、操作或验收时，只能使用浏览器类型为 `iab`、名称为 `Codex In-app Browser` 的 Codex 内置浏览器。
- Edge、Chrome 或其他外部浏览器即使打开了相同网址、显示相同标题或已经保存账号密码，也不得当作 Codex 内置浏览器使用；Codex 内置浏览器不可用时必须说明阻塞，不得静默切换外部浏览器。
- 首次打开页面以及任务中断、上下文恢复或客户端重启后，必须重新核对浏览器类型和当前页面；不得仅凭旧摘要、旧浏览器 ID、旧标签 ID 或页面标题判断浏览器身份。
- 长时间任务应复用并保留已经确认的 Codex 内置浏览器标签；标签已经失效或不存在时，只能在 Codex 内置浏览器中重新打开 Aimili Gateway 前端。
- 登录页的可访问性树可能不会显示输入框当前值，尤其是未获得焦点的密码字段；“可访问性文本没有显示密码”只能说明该层没有暴露字段值，不能证明浏览器没有保存或自动填充凭据。
- 检查登录状态时，先确认是否已经位于登录后的页面；确实停留在登录页时，再依次让字段获得焦点、查看同一时刻的页面截图和掩码显示、核对登录按钮状态，并以实际登录后的 URL 和页面内容作为最终依据。可访问性文本仅作辅助证据，不得覆盖截图、控件状态和真实提交结果。
- 只有在登录页完成上述检查后，截图仍明确显示字段为空且浏览器没有发生自动填充，才能判断“未保存凭据”；任何证据不一致或字段值受保护不可见时，只能标记为“尚未确认”，不得误报为“没有保存密码”。
- 如果登录页已有浏览器保存并自动填充的账号密码，只点击一次“登录”，随后根据网址、页面内容和登录结果判断是否成功；不得因为等待或状态不明确而改用外部浏览器。点击登录前不得读取、复制、输出或记录密码明文。
- 如果 Codex 内置浏览器没有保存账号密码，立即暂停需要登录的操作并请用户亲自在内置浏览器中填写和保存；用户确认完成后再继续原任务，不得自行猜测、提取或向其他浏览器复制凭据。
- 不在项目文件、任务记录、命令输出或回复中保存和披露账号、密码、Cookie、会话令牌及其他登录凭据。

## 生产安全边界

- Gateway 只管理 `agw-` 命名空间；不得删除、接管或修改非 Gateway 受管 3x-ui/Xray 资源。
- 不增加 AimiliVPN 实际运行出口数量；mixed/SOCKS5H 不随公网协议切换。
- “本地实现完成”“生产已部署”“用户原操作已验证通过”必须分别陈述，不得混称完成。
- 不输出或提交密码、Cookie、令牌、UUID、私钥、随机后台路径或完整订阅链接。
